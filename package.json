{
  "name": "ai-development-pipeline",
  "version": "1.0.0",
  "description": "Claude/VSCode/Augment AI pipeline with unified MCP, Vercel, Airtable, Square integration",
  "main": "local-mcp-server.ts",
  "scripts": {
    "dev": "npx ts-node local-mcp-server.ts",
    "start": "npx ts-node local-mcp-server.ts",
    "start:local": "npx ts-node local-mcp-server.ts",
    "build": "echo 'Build completed - using TypeScript files directly'",
    "cloud-mcp": "vercel dev",
    "deploy": "vercel deploy --prod",
    "install:global": "npm install -g ts-node nodemon",
    "lint": "npx eslint . --ext .ts",
    "test": "echo 'No tests yet.'",
    "type-check": "npx tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "express": "^4.19.2",
    "zod": "^3.22.4",
    "body-parser": "^1.20.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.3",
    "typescript": "^5.4.5",
    "@types/node": "^20.11.30",
    "@types/express": "^4.17.21",
    "ts-node": "^10.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "Claude",
    "AI",
    "MCP",
    "Vercel",
    "Airtable",
    "Augment",
    "pipeline"
  ],
  "license": "MIT",
  "author": "Tim Burger"
}
